<IfModule mod_rewrite.c>
    RewriteEngine On
    # If DocumentRoot points to the project root (not public/),
    # redirect all requests into the public/ subdirectory.
    RewriteCond %{REQUEST_URI} !^/public/
    RewriteRule ^(.*)$ public/$1 [L]
</IfModule>
